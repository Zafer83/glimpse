Here is the source code and documentation of a software project.

The content is organized by priority:
- DOCUMENTATION section: the project's purpose, goals, architecture.
- BUSINESS LOGIC section: core source code.
- SUPPORTING CODE section: tests, configs. Only for background context.

{{truncNote}}
PROJECT SOURCE:
{{code}}

END OF SOURCE CODE.

Now generate a Slidev Markdown presentation. Follow these rules EXACTLY:

RULES:
1. Output ONLY Slidev Markdown. NO explanations before or after.
2. NEVER copy documentation text verbatim. Rewrite everything as SHORT bullets (max 8 words each).
3. Every slide MUST start with # (single #, NOT ## or ###).
4. Each slide: MAX 5 bullets.
5. Section divider slides: output ONLY the # heading — no bullets, no extra text.
6. Include 1-2 code snippets from the source (inside ``` blocks).
7. Do NOT use mermaid blocks. Do NOT copy tables from docs.
8. Do NOT leave blank lines inside frontmatter blocks.
9. FRONTMATTER: between --- markers, ONLY key: value pairs. NO bullets, NO text.
10. For `image:` fields use 1-2 English keywords (e.g. `image: law,contract`), NOT URLs.

FIRST SLIDE (copy this structure exactly):
---
theme: '{{theme}}'
layout: cover
title: '{{projectName}}'
background: https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&w=1600&h=900&q=80
---

# {{projectName}}

{{projectDescription}}

THEN FOLLOW THIS PLAN:

{{slidePlan}}

LAST SLIDE (copy this structure exactly):
---
layout: cover
background: https://images.unsplash.com/photo-1516116216624-53e697fedbea?auto=format&fit=crop&w=1600&h=900&q=80
---

# Vielen Dank!

Fragen? Gerne jetzt!

AVAILABLE LAYOUTS:
- cover — Cover page (use with background: URL)
- default — Standard content slide
- section — Section divider (ONLY # heading, no content)
- image-right — Image right, content left (use with image: keywords)
- image-left — Image left, content right (use with image: keywords)
- two-cols — Two columns (use ::right:: separator)
- fact — Prominent fact or data point
- statement — Bold statement

Do NOT use layout: end. Use layout: section for topic dividers.
For two-cols use ::right:: to separate columns.

BEGIN OUTPUT NOW:
