Read the project documentation and source code below. Generate a PROFESSIONAL Slidev Markdown presentation.

IMPORTANT: The content is organized in three priority sections:
1. PROJECT DOCUMENTATION — Drives the presentation narrative. Extract key facts, numbers, and themes.
2. CORE BUSINESS LOGIC — Select interesting code patterns for technical slides.
3. SUPPORTING CODE — Background context only, do not feature prominently.

Base the presentation STORY on the documentation. Use code only to illustrate key technical points.

{{truncNote}}
PROJECT SOURCE:
{{code}}

END OF SOURCE CODE.

PRESENTATION REQUIREMENTS:

1. SLIDE COUNT: Generate 15-20 slides total.

2. FIRST SLIDE (cover with background):
---
theme: '{{theme}}'
layout: cover
title: '<your chosen title>'
background: https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&w=1600&h=900&q=80
---

# <Project Title>

<One-line description of the project>

---

3. LAST SLIDE (outro with background):
---
layout: cover
background: https://images.unsplash.com/photo-1516116216624-53e697fedbea?auto=format&fit=crop&w=1600&h=900&q=80
---

# Thank You

Questions? Feedback?

---

4. SECTION DIVIDERS — Use `layout: section` slides between major topics.
   These slides contain ONLY a # heading — no bullets, no content, no images.
   This creates visual rhythm and breathing room in the presentation.

Example:
---
layout: section
---

# Architecture

---

5. IMAGE SLIDES — At least 3 slides with `layout: image-right` or `image-left`.
   For `image:` fields write 1-2 descriptive English keywords (e.g. `image: cloud,server`), NOT full URLs.

Example:
---
layout: image-right
image: architecture,cloud
---

# System Architecture

- Microservice-based backend
- Event-driven communication
- Auto-scaling infrastructure

---

6. LAYOUT VARIETY — Use at least 5 different layout types across the presentation:
   cover, default, section, image-right, image-left, two-cols, fact, statement, quote

7. CONTENT QUALITY:
   - Use SPECIFIC numbers and statistics from the documentation (percentages, prices, metrics).
   - Each bullet: max 10 words. Each slide: max 5 bullets.
   - Include 2-3 real code snippets from the provided source code.
   - Use markdown tables for comparisons where appropriate.
   - NEVER copy documentation verbatim — synthesize and rephrase.

8. FORMAT RULES:
   - Every slide MUST start with # (top-level heading, NOT ## or ###).
   - No blank lines inside frontmatter blocks (between --- markers).
   - No mermaid code blocks (Slidev cannot render them reliably).
   - FRONTMATTER: only key: value pairs between --- markers. NO bullets or prose.
   - For two-cols layout: use ::right:: to separate columns.
   - Do NOT use layout: end — it does not exist. Use layout: cover for the last slide.
   - Do NOT wrap output in a markdown code fence.

{{slidePlan}}

OUTPUT ONLY SLIDEV MARKDOWN — no explanations, no commentary, no wrapper fences.
